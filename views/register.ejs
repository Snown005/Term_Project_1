<!DOCTYPE html>
<html lang="uk">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Реєстрація | Система вибору керівника</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/index.css">
</head>
<body class="min-h-screen flex items-center justify-center p-4">
  <div class="auth-card bg-green-100 rounded-lg shadow-lg p-8 w-96 md:w-[32rem]">
    <div class="text-center mb-8">
      <h1 class="text-2xl font-bold text-gray-800 mb-2">Реєстрація</h1>
      <p class="text-gray-600">Створіть обліковий запис для доступу до системи</p>
    </div>

    <% if (errorMessage) { %>
      <div class="mb-4 p-4 bg-red-50 border-l-4 border-red-500 text-red-700">
        <%= errorMessage %>
      </div>
    <% } else if (Messag) { %>
      <div class="mb-4 p-4 bg-green-50 border-l-4 border-green-500 text-green-700">
        <%= Messag %>
      </div>
    <% } %>

    <form action="/home/register" method="POST" class="space-y-6">
      <div>
        <label for="username" class="block text-sm font-medium text-gray-700 mb-1">
          Ім'я користувача
        </label>
        <input 
          type="text" 
          id="username" 
          name="username" 
          required 
          class="auth-input border-2 border-gray-300 hover:border-yellow-500"
          placeholder="Оберіть ім'я користувача"
        >
      </div>
      
      <div>
        <label for="password" class="block text-sm font-medium text-gray-700 mb-1">
          Пароль
        </label>
        <input 
          type="password" 
          id="password" 
          name="password" 
          required 
          class="auth-input border-2 border-gray-300 hover:border-yellow-500"
          placeholder="Створіть надійний пароль"
        >
      </div>

      <button type="submit" class="auth-button hover:bg-green-600 hover:text-white">
        Зареєструватися
      </button>
    </form>

    <div class="mt-6 text-center">
      <p class="text-gray-600">
        Вже маєте обліковий запис? 
        <a href="/login" class="auth-link font-medium hover:bg-blue-400 hover:text-white">
          Увійти
        </a>
      </p>
    </div>
  </div>
</body>
</html>